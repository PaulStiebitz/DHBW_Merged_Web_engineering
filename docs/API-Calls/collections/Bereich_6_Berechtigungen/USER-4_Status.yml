info:
  name: USER-4 User sperren entsperren
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:3000/api/users/3/status
  body:
    type: json
    data: |-
      {
        "is_locked": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
